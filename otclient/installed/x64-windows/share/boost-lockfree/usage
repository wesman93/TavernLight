The package boost-lockfree is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS lockfree)
    target_link_libraries(main PRIVATE Boost::lockfree)

or the generated cmake configs via:

    find_package(boost_lockfree REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::lockfree)

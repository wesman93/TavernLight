The package boost-tti is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS tti)
    target_link_libraries(main PRIVATE Boost::tti)

or the generated cmake configs via:

    find_package(boost_tti REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::tti)
